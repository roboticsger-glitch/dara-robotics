import Image from "next/image";
import Link from "next/link";
import { notFound } from "next/navigation";
import { ArrowRight, CheckCircle2 } from "lucide-react";
import { getProduct } from "@/lib/data";

export default async function ProductPage({params}:{params:Promise<{slug:string}>}){ const {slug}=await params; const p=await getProduct(slug); if(!p) notFound(); return <><section className="product-detail"><div className="container product-detail-grid"><div className="detail-image"><Image src={p.image_url} alt={p.name} fill sizes="50vw"/></div><div><span className="eyebrow">{p.category}</span><h1>{p.name}</h1><p className="lead">{p.short_description}</p><div className="chips big">{p.service_modes.map(m=><span key={m}><CheckCircle2 size={15}/>{m}</span>)}</div><p>{p.description}</p><div className="spec-grid">{p.payload&&<div><span>Payload</span><b>{p.payload}</b></div>}{p.runtime&&<div><span>Runtime</span><b>{p.runtime}</b></div>}{p.navigation&&<div><span>Navigation</span><b>{p.navigation}</b></div>}{p.lead_time&&<div><span>Typical lead time</span><b>{p.lead_time}</b></div>}</div><Link className="button" href={`/request-quote?product=${encodeURIComponent(p.name)}`}>Request pricing & assessment <ArrowRight size={17}/></Link></div></div></section><section className="section alt"><div className="container split"><div><span className="eyebrow">BEST FIT</span><h2>Typical industries</h2><div className="tag-cloud">{p.industries.map(i=><span key={i}>{i}</span>)}</div></div><div><h3>What your project can include</h3><ul className="check-list"><li>Site survey and workflow design</li><li>Robot sourcing and configuration</li><li>Installation and commissioning</li><li>Staff training</li><li>Preventive maintenance</li><li>Spare-parts and support plan</li></ul></div></div></section></> }
